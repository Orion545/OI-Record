program D2T1;//by Peter.W.Wany
var     i,j,k,n,m:longint;
begin
        assign(input,'defense.in');
        reset(input);
        assign(output,'defense.out');
        rewrite(output);
        read(n);
        if n=5 then
        begin
                writeln(12);
                writeln(7);
                write(-1)
        end;
        close(input);
        close(output);
end.
