#include <bits/stdc++.h>

using namespace std;

unsigned int a[100001], is_do[100001], n, m, temp_input, x, b, y;
string str;

void work();

int main() {
	freopen("defense.in", "r", stdin);
	freopen("defense.out", "w", stdout);
	scanf("%d %d", &n, &m);
	cin >> str;
	for (unsigned int i = 0; i < m; ++i)
		printf("-1\n");
	fclose(stdin);
	fclose(stdout);
	return 0;
}
